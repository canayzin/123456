# NovaBase RTDB Service (Phase 2)

Planned APIs:
- `GET/PUT/PATCH/DELETE /v1/projects/:pid/rtdb/*path`
- `WS /v1/projects/:pid/rtdb/subscribe`

Model choice: JSON tree over key-value namespaces with path-based rules.
