# Copy to .npmrc and set reachable internal registry/proxy.
registry=https://<YOUR_ARTIFACTORY_OR_NEXUS>/api/npm/npm/
always-auth=true
//<YOUR_ARTIFACTORY_OR_NEXUS>/:_authToken=${NPM_TOKEN}

# Optional: if your proxy is scoped
# @novabase:registry=https://<YOUR_ARTIFACTORY_OR_NEXUS>/api/npm/npm/
