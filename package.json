{
  "name": "novabase",
  "private": true,
  "version": "0.2.0",
  "workspaces": [
    "services/*",
    "sdk/js",
    "examples/todo-web",
    "console"
  ],
  "scripts": {
    "build": "npm run -ws build",
    "test": "npm run -ws test",
    "test:coverage": "node --test --experimental-test-coverage services/rules/src/index.test.js services/docdb/src/query.test.js",
    "lint": "eslint .",
    "format": "prettier --write .",
    "prepare": "husky"
  },
  "devDependencies": {
    "@types/node": "^22.13.4",
    "eslint": "^9.20.0",
    "husky": "^9.1.7",
    "prettier": "^3.5.1",
    "typescript": "^5.7.3"
  }
}
